import Layout from '@/components/layouts';
import SuperRep from '@/components/tronsr';

const TronSRPage = props => {
  return (
    <Layout>
      <SuperRep {...props} />
    </Layout>
  );
};

export default TronSRPage;
