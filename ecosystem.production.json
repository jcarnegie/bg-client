{
  "apps": [
    {
      "name": "PortalClient",
      "script": "build/server/server.js",
      "exec_mode": "cluster",
      "env": {
        "NODE_ENV": "production",
        "PORT": "5000",
        // TODO switch back to 'server' once we have proper staging
        "RENDERING": "client"
      }
    }
  ]
}
